#include "QuadricSurfaceDescriptor.h"

const QuadricSurfaceDescriptor SOLID(GLU_FILL);
const QuadricSurfaceDescriptor WIREFRAME(GLU_LINE);
const QuadricSurfaceDescriptor VERTICES(GLU_POINT);
const QuadricSurfaceDescriptor SILHOUETTE(GLU_SILHOUETTE);
